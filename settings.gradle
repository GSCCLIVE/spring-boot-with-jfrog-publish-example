pluginManagement {
	repositories {
		maven {
			url "${artifactory_contextUrl}/maven-group"
			credentials {
				username = "${artifactory_user}"
            	password = "${artifactory_password}"
			}
		}
	}
}

rootProject.name = 'sample-spring-boot'